# cosmos-plugins plugin fix report

- Plugin: cosmos-plugins (cosmos-sources-tools 0.9.0; cosmos-stockdata-tools 0.5.0)
- Scope: plugin
- Timestamp (UTC): 2026-08-25T09:48:34Z
- Status: repaired

## Why the change was needed

The HTML attachment capability was developed before the daily-output layout but touched overlapping ZSXQ contracts, documentation, and tests. Integrating the branches in the wrong semantic order could downgrade the source plugin, lose stable file-card checks, or leave an HTML test on the retired flat output path.

## Reproduction

1. Merge the HTML attachment branch into the daily-output branch.
2. Resolve the shared architecture, decision, gotcha, and source-manifest files.
3. Run the combined ZSXQ suite. The HTML runner fixture must use a direct file below the Beijing-date directory, while all HTML identity and extraction behavior remains intact.

## Changed packaged paths

- `ARCHITECTURE.md`: retained both PDF/HTML extraction architecture and the daily-output lock/layout architecture.
- `DECISION.md`: retained both the HTML attachment decision and the newer stable daily-output identity decision.
- `GOTCHAS.md`: retained both file-card identity and generated-output identity safeguards.
- `plugins/cosmos-sources-tools/.codex-plugin/plugin.json`: retained the final source-plugin version 0.9.0.
- `plugins/cosmos-sources-tools/skills/zsxq-fetch/tests/zsxq-runner.test.mjs`: moved the merged HTML fixture under the required date directory.

## Diff summary

The final combined tree preserves the complete 0.8.0 HTML capability as part of source plugin 0.9.0, then layers date-directory output, canonical planet identity, same-target locking, freshness ordering, and complete-result protection on top. No HTML implementation or daily-output implementation was dropped.

## Validation

- `node --test tests/*.test.mjs`: exit 0; 80/80 passed.
- `node --test plugins/cosmos-sources-tools/skills/zsxq-fetch/tests/*.test.mjs`: exit 0; 71/71 passed.
- Python unit suites in the `cosmos` environment: exit 0; 17/17, 68/68, and 43/43 passed.
- `quick_validate.py` for CLS, DingTalk, Feishu, ZSXQ, and stockdata Skills: exit 0; all five valid.
- `node --check` for the ZSXQ browser collector and `git diff --check`: exit 0.
- Independent read-only integration review: no P1, P2, or P3 findings; the final file set equals the expected union of both branches.

## Limitations and privacy notes

- The first Python attempt used an obsolete environment that lacked declared conversion dependencies; the same suites passed without code changes in the project's designated `cosmos` environment.
- No live authenticated ZSXQ fixture was used during this integration verification.
- The report contains no user private content, credentials, local workspace roots, or business identifiers.
- This follow-up supersedes the earlier daily-output report's integration limitation: the HTML capability is now merged first and preserved in the 0.9.0 combined tree.
