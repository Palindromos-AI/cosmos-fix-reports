# cosmos-sources-tools zsxq-fetch fix report

- Plugin: cosmos-sources-tools 0.19.5 (unreleased)
- Scope: zsxq-fetch
- Timestamp (UTC): 2026-09-04T02:26:19Z
- Status: repaired

## Why the change was needed

The image instructions prioritized rendered previews and screenshots instead of retaining three successful file-acquisition methods. The approved procedure requires ordered acquisition, followed by native-resolution local reading without redundant downloads.

## Reproduction

Prior interactive testing acquired viewer-loaded image files through browser resource export, browser image download, and the existing single-asset export script. Screenshot attempts timed out. The tiling helper reported a missing sharp dependency; no dependency was installed.

## Changed packaged paths

- `skills/zsxq-fetch/SKILL.md`: order resource export, browser download, and existing export script; remove screenshot transcription; share native-resolution reading, failure classification, and cleanup.
- `.codex-plugin/plugin.json`: increment version from 0.19.4 to 0.19.5.

Development-only changes add an ordering regression test and update ARCHITECTURE.md, CHANGELOG.md, DECISION.md, and GOTCHAS.md.

## Diff

- Removed preview/screenshot-first image extraction.
- Added pageAssets, then downloadMedia or visible download control, then zsxq-single-asset-export.mjs.
- Advance only after acquisition failure or unavailability; stop after obtaining the correct complete file.
- Preserve authentication and permission stop rules, native-resolution reading, optional lossless tiling, dependency approval, failure notes, and exact temporary-artifact cleanup.

## Validation

- `node --test --test-name-pattern='ZSXQ image acquisition' tests/cosmos-sources-tools.test.mjs`: exit 1 before implementation, expected regression failure.
- `node --test --test-reporter=dot tests/*.test.mjs`: passed with no failure markers.
- `node --test --test-reporter=dot plugins/cosmos-sources-tools/skills/zsxq-fetch/tests/*.test.mjs`: passed with no failure markers.
- `micromamba run -n cosmos python -B <skill-creator>/scripts/quick_validate.py plugins/cosmos-sources-tools/skills/zsxq-fetch`: exit 0, Skill is valid.
- `git diff --check`: exit 0.
- Independent read-only review found no correctness or unintended-scope issues; parent inspected the image diff.

## Limitations

No new interactive collection was run during this modification. Prior acquisition success does not establish coverage of all image formats, ultra-long images, or scientific charts. Tiling remains unverified at runtime because sharp was unavailable. The source tree began clean; no unrelated modifications existed. Feishu, DingTalk, non-image extraction, and runtime scripts are unchanged. Source changes remain uncommitted and unreleased; installed caches are unchanged.
