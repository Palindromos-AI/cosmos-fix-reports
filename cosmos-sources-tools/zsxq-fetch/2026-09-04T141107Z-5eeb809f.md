# cosmos-sources-tools zsxq-fetch release fix report

- Plugin: cosmos-sources-tools 0.20.5
- Scope: zsxq-fetch
- Timestamp (UTC): 2026-09-04T14:11:07Z
- Status: repaired

## Why the change was needed

Release the previously validated local change that routes every topic link through the general webpage extraction flow. The release also removes obsolete detail-only test contracts and bumps the independently versioned sources plugin from 0.20.4 to 0.20.5.

## Reproduction

The previous instructions routed a linked platform topic to a dedicated detail parser, which could fail with DETAIL_ROOT_MISMATCH. General webpage extraction read the same linked body in the prior live validation. The user explicitly approved committing, merging, pushing, and publishing this change.

## Changed packaged paths

- `.codex-plugin/plugin.json`: bump sources plugin version to 0.20.5.
- `skills/zsxq-fetch/SKILL.md`: all topic links use the existing webpage flow.
- `skills/zsxq-fetch/references/runner-contract.md`: remove detail APIs and describe one linked-page flow.
- `skills/zsxq-fetch/scripts/zsxq-browser-collector.mjs`: remove dedicated detail parsing/download helpers and selectors, retain timeline/direct-attachment implementations, use contract v13.

Development-only changes: integrate the rule into the existing four maintainer documents, add six linked-page regression tests, and remove tests/fixtures exclusive to the deleted detail APIs. All retained timeline and direct-download behavior tests remain unchanged except two contract-version assertions.

## Diff

```diff
- "version": "0.20.4"
+ "version": "0.20.5"
- Platform topic links use a dedicated detail collector.
+ Every topic link uses the same general webpage extraction flow.
```

## Validation

- `node --test tests/*.test.mjs`: exit 0, 120 tests passed.
- `node --test plugins/cosmos-sources-tools/skills/zsxq-fetch/tests/*.test.mjs`: exit 0, 93 tests passed.
- `micromamba run -n cosmos python -B -m unittest discover -s tests -q`: exit 0 in llm-wiki (17), raw-to-markdown (68), and stockdata-fetch (40); 338 total tests passed across the five suites.
- Official skill and plugin validators in the configured Python environment: exit 0.
- JavaScript syntax checks and `git diff --check`: exit 0.
- Independent medium release review: no verifiable issue; 50 focused tests passed. Only detail-exclusive tests and fixtures were removed.
- Release archive inspection: exactly the four intended packaged paths differed; no tests, maintainer documents, or tools were included.
- `tools/publish-marketplace.sh main`: exit 0; marketplace snapshot committed and pushed.
- Whole-history audit after fetching the marketplace ref: 84 unique paths inspected, no development-only paths present; published manifest version is 0.20.5.

## Limitations

- Development commit `ad2d742` was fast-forward merged to main and pushed with explicit user authorization. Marketplace snapshot commit: `451604c190cffea2d61037f06790e08ed9867b72`.
- No new live recrawl was performed during release; the prior live check established readability of the formerly failed linked body, not all image/download branches.
- An unrelated checkout with existing edits was discovered and left untouched. No unrelated modifications were included.
- Both task worktrees were removed after the runtime patch, tests, and decisions were incorporated into the private development repository. The marketplace receives only the sanctioned export snapshot.
- This report's publisher commits and pushes only this report; source and marketplace publication were separate, user-authorized actions.
