# cosmos-sources-tools zsxq-fetch fix report

- Plugin: cosmos-sources-tools 0.20.4 (unreleased)
- Scope: zsxq-fetch
- Timestamp (UTC): 2026-09-04T13:04:50.960Z
- Status: repaired

## Why the change was needed

The previous unpublished linked-page helper required identical body/link/image snapshots and complete document loading. Dynamic ancillary content could prevent success even when article text was readable. The user approved replacing stability checks with readable main-content availability.

## Reproduction

A readable page with a changing counter never satisfies identical signatures. A short article can remain readable while advertisements load. Earlier failures exposed only a generic body-unavailable code.

## Changed packaged paths

- `skills/zsxq-fetch/scripts/zsxq-linked-page-collector.mjs`: remove consecutive-snapshot comparison, minimum delay and readyState completion requirement; return readable content immediately; retry blank/loading-only/access-challenge states; distinguish read and navigation failures. Exclude explicit navigation and hidden cached text from the readiness text.
- `skills/zsxq-fetch/tests/zsxq-linked-page-collector.test.mjs`: test changing content, short readable articles with loading widgets, pure placeholders, hidden caches, image-only pages, and navigation timeout recovery.
- `skills/zsxq-fetch/SKILL.md`: do not classify dynamic changes as failures; retain useful content and identify only actual unread portions.
- `skills/zsxq-fetch/references/runner-contract.md`: simplify helper options and document specific causes without changing Runner input.
- Maintainer-only architecture, gotchas, decisions, changelog and integration-test wording synchronized.

## Diff

Remove stableSamples, minimumWaitMs, signature comparison and document-ready completion checks. Return on the first usable snapshot instead. Preserve existing filename-link behavior, image workflow, official topic attachment downloads and Runner completeness calculation.

## Validation

- `node --test plugins/cosmos-sources-tools/skills/zsxq-fetch/tests/*.test.mjs tests/*.test.mjs`: exit 0, 221 tests passed.
- `node --check plugins/cosmos-sources-tools/skills/zsxq-fetch/scripts/zsxq-linked-page-collector.mjs`: exit 0.
- `git diff --check`: exit 0.
- Skill creator quick validation in existing cosmos Python environment: exit 0.
- Independent read-only review: navigation/loading and hidden-cache cases reproduced; corrected fixture and regression tests failed before fixes, then passed. Final hidden-cache reproduction correctly remains loading.
- Final single-page Chrome live check with default options and a fresh module import: complete in 2561 ms, 4765 body characters containing the article, zero document candidates, five image candidates. No files or images downloaded. Owned tab closed; old reports unchanged.
- No separate lint or build command is defined for the affected runtime.

## Limitations

This was a targeted page check, not a new full collection. Success does not establish the historical failure's unique cause. Generic DOM structure cannot guarantee semantic article completeness; the agent still checks that returned content is the article rather than navigation. Image candidates still require substantive-image selection. Existing same-task uncommitted edits were preserved; no unrelated paths were changed. No new files were introduced in this follow-up. Version stays 0.20.4 because the feature is unpublished. Source remains uncommitted in its feature worktree and the installed cache is unchanged.
