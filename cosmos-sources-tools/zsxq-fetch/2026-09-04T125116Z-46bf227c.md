# cosmos-sources-tools zsxq-fetch fix report

- Plugin: cosmos-sources-tools 0.20.4
- Scope: zsxq-fetch
- Timestamp (UTC): 2026-09-04T12:51:16.011Z
- Status: repaired

## Why the change was needed

External-page file discovery varied between runs, and plain-text browser extraction lost anchors. The approved behavior keeps directly linked PDF, HTML, and Word filenames and safe full source URLs in the page body without downloading or parsing those files. Direct topic attachments and image reading remain unchanged.

## Reproduction

A linked article containing document anchors can return text without URLs when read using innerText. A dynamic placeholder can also be accepted before the article arrives. A filename merge that only checks URL presence leaves generic labels instead of filenames.

## Changed packaged paths

- `.codex-plugin/plugin.json`: version 0.20.3 to 0.20.4.
- `skills/zsxq-fetch/SKILL.md`: external linked files stay in body; linked-page stabilization and existing failure reporting.
- `skills/zsxq-fetch/references/runner-contract.md`: helper usage and image-only new embedded-media collection; unchanged runner data structures.
- `skills/zsxq-fetch/scripts/zsxq-linked-page-collector.mjs`: bounded rendered-state sampling, anchor preservation, filename normalization, deduplication and safe URL handling.
- `skills/zsxq-fetch/tests/zsxq-linked-page-collector.test.mjs`: regression tests for links, dynamic placeholders, challenge pages, image-only content and signed URL cleanup.
- Maintainer-only integration tests and architecture, decisions, gotchas, changelog were updated in the development repository.

## Diff

Summary instead of full diff: add linked-page helper; replace external document download/extraction instructions with filename-bearing links in body; preserve image extraction and official topic-file downloads. No Runner implementation or completeness-calculation changes.

## Validation

- `node --test plugins/cosmos-sources-tools/skills/zsxq-fetch/tests/*.test.mjs tests/*.test.mjs`: exit 0, 216 passed.
- `node --check plugins/cosmos-sources-tools/skills/zsxq-fetch/scripts/zsxq-linked-page-collector.mjs`: exit 0.
- `git diff --check`: exit 0.
- Skill creator quick validation through the existing cosmos Python environment: exit 0. Initial system-Python attempt lacked PyYAML; no dependency was installed.
- Independent read-only review reproduced filename, signed-autolink and placeholder issues; regression tests failed before fixes and passed afterwards. Image-only compatibility received an additional regression test.
- Parallel live full and image-excluded validation: each observed 12 topics, 5 PDF and 3 HTML links, no non-image document downloads and no embedded document sections. Existing raw results were reassembled with the corrected merge helper through the normal publisher.
- No standalone lint/build script exists for the affected runtime; syntax checks and test suites were used.

## Limitations

Live outputs remain incomplete: one external page did not stabilize although clean body extraction succeeded; full extraction also reported five unreadable small image items. These are not reported as successful extraction. The final readiness refinements were covered by regression tests, not a new complete live collection. Word-link behavior is covered by fixtures, not by this live window. Stable samples are an operational readiness heuristic, not proof that all future dynamic content has arrived. Source edits remain uncommitted in a feature worktree; installed cache is unchanged. New helper and tests belong in source control, not gitignore. No unrelated initial working-tree modifications existed.
