# cosmos-sources-tools dingding-fetch fix report

- Plugin: cosmos-sources-tools 0.19.4 (unreleased)
- Scope: dingding-fetch
- Timestamp (UTC): 2026-09-04T02:00:41Z
- Status: repaired

## Why the change was needed

The user requested that DingTalk adopt the same download-only image extraction workflow already added to Feishu, without separate download approval or the old viewer fallback.

## Reproduction

Instruction comparison showed that DingTalk still used viewer zoom and screenshots and required separate export approval. No DingTalk live collection was performed.

## Changed packaged paths

- `plugins/cosmos-sources-tools/skills/dingding-fetch/SKILL.md`: route image acquisition to official original downloads without separate approval.
- `plugins/cosmos-sources-tools/skills/dingding-fetch/references/image-extraction.md`: synchronize acquisition, original-resolution reading, failure handling, source preservation, and cleanup with Feishu.
- `plugins/cosmos-sources-tools/.codex-plugin/plugin.json`: prepare patch version 0.19.4.

## Diff

Removed viewer/thumbnail/screenshot extraction and the separate export-approval requirement. Preserve explicit user restrictions and system permissions. Restored full normalized image-reference equality checks in development tests; updated maintainer documents with the live-test limitation.

## Validation

- `node --test tests/chat-skill-drift.test.mjs`: initially 3 failures against the old workflow; 5 passed after the update.
- `node --test tests/*.test.mjs`: exit 0, 103 passed.
- `quick_validate.py <dingding-skill>` in the project Python environment: exit 0, valid.
- `git diff --check`: exit 0.
- Independent read-only review: no actionable issues; 5 focused tests passed independently.

## Limitations

DingTalk's acquisition controls have not been live-tested. The downloaded-original route was previously verified only in Feishu. Source changes remain uncommitted in a feature worktree; no source push, marketplace publication, or installed-cache change was performed. No unrelated working-tree modifications existed.
