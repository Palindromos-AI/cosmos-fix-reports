# cosmos-sources-tools feishu-fetch fix report

- Plugin: cosmos-sources-tools 0.19.3
- Scope: feishu-fetch
- Timestamp (UTC): 2026-09-04T01:50:02Z
- Status: repaired

## Why the change was needed

The user requested the previously verified original-download workflow be applied to marketplace source and pushed, replacing the old image-reading procedure rather than changing only an installed cache.

## Reproduction

Use the official image context menu to download the original, then inspect the saved image locally at native resolution. Earlier live testing verified this route while separate-window selection was unreliable.

## Changed packaged paths

- `plugins/cosmos-sources-tools/skills/feishu-fetch/SKILL.md`: original downloads are the sole acquisition route, without separate approval; scope exclusions and system permissions remain.
- `plugins/cosmos-sources-tools/skills/feishu-fetch/references/image-extraction.md`: official download, file association, native-resolution reading, fail-visible completeness, and temporary cleanup replace viewer/screenshot extraction.
- `plugins/cosmos-sources-tools/.codex-plugin/plugin.json`: patch version 0.19.3.

## Diff

Removed viewer, thumbnail, and screenshot extraction fallbacks. Acquisition and local-reading failures make the group incomplete. Removed legacy chart-label extraction wording. Maintainer-only docs and focused tests record the intentional Feishu/DingTalk acquisition difference without changing DingTalk.

## Validation

- `node --test tests/*.test.mjs`: exit 0, 102 passed.
- `node --test plugins/cosmos-sources-tools/skills/zsxq-fetch/tests/*.test.mjs`: exit 0, 89 passed.
- Python unittest suites in the project environment: exit 0, 17 wiki, 68 conversion, 40 stockdata tests passed.
- `quick_validate.py <feishu-skill>` in the project environment: exit 0, valid.
- `git diff --check`: exit 0.
- Independent review: source behavior matched the request; narrowed test exclusions to retain shared fidelity and completeness protection, then reran four focused tests successfully.

## Limitations

No unrelated working-tree modifications existed. This is a documentation workflow change backed by the earlier live test; no new live collection was needed. Source commit and remote publication are separately authorized by the user's request and are not performed by the report publisher. User data and source messages are excluded from this report.
