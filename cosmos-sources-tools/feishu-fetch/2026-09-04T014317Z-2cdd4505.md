# cosmos-sources-tools feishu-fetch fix report

- Plugin: cosmos-sources-tools 0.19.2
- Scope: feishu-fetch
- Timestamp (UTC): 2026-09-04T01:42:16Z
- Status: repaired

## Why the change was needed

The desktop automation could not reliably select a separate image window. The official download-original action followed by local native-resolution reading was verified successfully. The user requested that this become the sole image extraction workflow without separate download approval.

## Reproduction

In an in-scope group, open an image context menu, choose download original, save the file, and read it locally at original resolution. The tested image was fully legible by this route.

## Changed packaged paths

- `skills/feishu-fetch/SKILL.md`: route image extraction through temporary original downloads without separate approval; retain user restrictions and system permissions.
- `skills/feishu-fetch/references/image-extraction.md`: replace thumbnail/viewer/screenshot extraction with official download and local reading; document destination verification, failure handling, and temporary-file cleanup. Remove conflicting chart-label extraction wording.

## Diff

- Removed viewer zoom, screenshot fallback, and separate export-approval requirement.
- Added official download-original acquisition, per-message file association, native-resolution inspection, and scoped cleanup.
- Download or local-reading failure now results in incomplete status without the old fallback routes.

## Validation

- `quick_validate.py <skill-dir>`: exit 1 under both available Python runtimes; PyYAML is not installed, so the validator could not start.
- Focused Node structure and workflow consistency checks: exit 0.
- Independent read-only review: identified chart-label wording conflicting with the existing chart omission rule; confirmed and corrected.
- Live official download and native-resolution reading: successful before the instruction update; no second live collection was performed for this documentation-only change.

## Limitations

Only the installed skill files were changed; no marketplace source repository was committed, pushed, or published. A plugin update may replace these local changes. Unrelated installed-package modifications were not audited or attributed to this task. System permission prompts still apply. Full Python skill validation remains unavailable due to missing PyYAML.
