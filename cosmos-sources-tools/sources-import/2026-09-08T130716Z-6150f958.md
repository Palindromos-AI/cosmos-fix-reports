# cosmos-sources-tools sources-import fix report

- Plugin: cosmos-sources-tools 0.21.0
- Scope: sources-import
- Timestamp (UTC): 2026-09-08T13:07:16.935Z
- Status: repaired

## Why the change was needed

Package a confirmed raw-library intake workflow under the sources-import name. Previously the sources plugin collected content but did not provide selection-and-confirmation intake for existing source and stockdata artifacts.

## Reproduction

Invoke sources-import with no date: list files collected today in Beijing time, accept additions/removals, and copy only the confirmed selection into the raw library. This is a new capability, not a defect reproduction.

## Changed packaged paths

- skills/sources-import/SKILL.md: portable home-relative intake workflow, provenance, incomplete/empty snapshot disclosure, duplicate handling, exclusive creation, and content verification; standard packaged-change reporting trigger.
- skills/sources-import/agents/openai.yaml: display name and invocation prompt.
- .codex-plugin/plugin.json: version 0.21.0 and discovery metadata.
- README.md (marketplace root): user-facing capability description.
- tests/sources-import.test.mjs and tests/cosmos-fix-tools.test.mjs (development only): discovery and reporting coverage.

## Diff

Added sources-import instructions and UI metadata. Plugin version changed from 0.20.6 to 0.21.0. Existing collectors and their output contracts remain unchanged.

## Validation

- quick_validate.py and validate_plugin.py: exit 0.
- node --test tests/*.test.mjs: exit 0, 122 passed.
- node --test plugins/cosmos-sources-tools/skills/zsxq-fetch/tests/*.test.mjs: exit 0, 93 passed.
- micromamba run -n cosmos python -B -m unittest discover -s tests -q: exit 0 for llm-wiki (34), raw-to-markdown (68), and stockdata-fetch (40).
- git diff --check: exit 0.
- Privacy scan of new skill files: no maintainer home paths or private source identifiers.

## Limitations

Instruction-only workflow; discovery and existing regressions are tested, but no live data was imported for release validation. Read-only candidate listing was exercised before packaging. No unrelated local modifications existed. New skill and test files belong in version control; existing ignore rules already cover generated output and worktrees.
