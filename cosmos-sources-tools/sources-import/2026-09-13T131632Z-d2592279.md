# cosmos-sources-tools sources-import fix report

- Plugin: cosmos-sources-tools 0.21.2 (unreleased)
- Scope: sources-import
- Timestamp (UTC): 2026-09-13T13:16:32.795Z
- Status: repaired

## Why the change was needed

The user requested content-date-only intake. The previous skill offered collection-task date and content date, defaulting to collection time. Selection must now use content date regardless of when collection ran.

## Reproduction

Request intake without a date: list existing material whose content date is today in Beijing time. A historical date request selects that content date across collection runs. A missing collection timestamp must not exclude content with a known date.

## Changed packaged paths

- skills/sources-import/SKILL.md: remove collection-date filtering and mode selection, default to content date, retain optional collection timestamps as provenance, and preserve multi-day extraction.
- skills/sources-import/agents/openai.yaml: content-date invocation prompt.
- .codex-plugin/plugin.json: version 0.21.2 and consistent discovery text.
- README.md (marketplace root): content-date-only capability description.

## Diff

Replaced the dual-date selection contract with content-date-only selection. Updated metadata and documentation; no runtime code changes.

## Validation

- quick_validate.py: exit 0.
- validate_plugin.py: exit 0.
- node --test tests/sources-import.test.mjs tests/cosmos-fix-tools.test.mjs: exit 0, 11 passed.
- git diff --check: exit 0.
- Stale wording scan of the current skill, UI metadata, README and architecture found no collection-date defaults.

## Limitations

Instruction-only change; no live data imported. Source changes remain uncommitted and unpublished pending user approval. No unrelated modifications. Existing development documentation was updated to reflect the requested date policy; historical changelog entries remain unchanged.
