# cosmos-methodology-tools plugin fix report

- Plugin: `cosmos-methodology-tools` `0.1.0`
- Scope: `plugin`
- Timestamp (UTC): `2026-08-26T141359Z`
- Status: repaired

## Why the change was needed

The initial methodology workflow could be read as reconstructing an updated document from the current conversation, and it asked for a separately selected methodology directory. Expected behavior is incremental improvement from the complete existing document and a fixed methodology subtree under the Cosmos plugin family's shared workspace root. Independent review also reproduced a concurrent rebinding race that could make the final binding nondeterministic.

## Reproduction

1. Read the initial update instructions and observe that they did not explicitly forbid replacing still-valid prior content with a current-conversation-only draft.
2. Inspect the initial configuration command and observe that it accepted an arbitrary methodology root.
3. Start two authorized rebinds from the same reviewed binding; without a compare-and-swap precondition, both could report success depending on scheduling.

## Changed packaged paths

- `skills/methodology-assistant/SKILL.md`: defined updates as incremental improvements from the complete selected document, fixed the library at `<cosmos-workspace-root>/methodologies`, and documented compare-and-swap rebinding.
- `skills/methodology-assistant/scripts/methodology-library.mjs`: derived the fixed methodology subtree, stored the shared workspace root, serialized binding changes with saves, and required the previously reviewed root for authorized reconfiguration.
- `.codex-plugin/plugin.json`: aligned discovery metadata with incremental improvement and shared-workspace behavior.
- `<marketplace-root>/tests/cosmos-methodology-tools.test.mjs`: added regression coverage for non-replacement instructions, fixed workspace derivation, save-versus-rebind exclusion, and concurrent rebind compare-and-swap behavior.
- `<marketplace-root>/README.md`: documented the corrected update and workspace contracts.
- `<marketplace-root>/ARCHITECTURE.md`: documented fixed workspace derivation and binding-lock order.
- `<marketplace-root>/CHANGELOG.md`: recorded the corrected packaged behavior.
- `<marketplace-root>/DECISION.md`: made incremental preservation and compare-and-swap rebinding project constraints.
- `<marketplace-root>/GOTCHAS.md`: recorded the replacement and concurrency failure modes and prevention rules.

## Diff

Omitted: the correction spans instructions, runtime code, tests, and repository documentation; the changed behavior is summarized above and no private content is needed.

## Validation

- `node --test tests/*.test.mjs`: exit 0, 89 tests passed.
- `node --check plugins/cosmos-methodology-tools/skills/methodology-assistant/scripts/methodology-library.mjs`: exit 0, syntax valid.
- Official skill validator: exit 0, skill is valid.
- Official plugin validator: exit 0, plugin validation passed.
- `git diff --check`: exit 0, no whitespace errors.
- Independent review and repeated concurrency exercise: exit 0, original P1 closed and no P0-P3 findings remained.

## Limitations

- No real methodology workspace or runtime binding was configured or mutated during package development; all behavioral tests used isolated temporary fixtures.
- The source worktree already contained the uncommitted initial plugin addition from the same parent task. This report covers only the follow-up behavior corrections; no unrelated local modifications were present.
- The marketplace source repository remains uncommitted, unmerged, and unpushed pending explicit user confirmation.
